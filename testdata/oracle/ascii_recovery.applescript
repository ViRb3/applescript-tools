on characterProbe()
    set numericValues to {65, 85, 120}
    set builtText to (ASCII character 65) & (ASCII character 85) & (ASCII character 120)
    return {numericValues, builtText}
end characterProbe
